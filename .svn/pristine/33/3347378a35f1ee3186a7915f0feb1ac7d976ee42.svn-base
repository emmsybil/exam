package com.cw.portal.service;

import java.util.List;

import org.apache.ibatis.annotations.Param;

import com.cw.common.domain.dictation.Dictation;
import com.cw.common.domain.dictation.DictationCase;

public interface DictationService {

	/**
	 * 获得所有信息,暂时没用
	 * 
	 * @return
	 */
	public List<DictationCase> getallDictationCase();

	/**
	 * 根据学生ID查询信息，暂时没用
	 * 
	 * @param studentId
	 * @return
	 */
	public Dictation getDictationByStudentId(@Param("studentId") int studentId);

	/**
	 * 根据时间段查询信息
	 * 
	 * @param studentId
	 * @return
	 */
	public List<Dictation> getDictationByDate(@Param("startTime") String startTime, @Param("endTime") String endTime);
	
}
